import { EconHighlightDirective } from './econ-highlight.directive';

describe('EconHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new EconHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
